## Codebase Patterns
- This repository is Python-first; use `python -m mypy docos/` for type checking and `python -m pytest tests/ -q` for automated verification.
- The main runtime boundaries are `docos/cli`, `docos/pipeline`, `docos/wiki`, `docos/lint`, `docos/harness`, and `docos/review`; keep stories scoped to one boundary when possible.
- Test files follow naming pattern `tests/test_<feature>_usXXX.py`
- US-001 through US-036 features may have been implemented in a previous PRD cycle — always check existing code and tests first
- Config YAML for tests: use inline strings with `environment: local\nschema_version: '1'` header
- Registered parsers: `stdlib_pdf` and `basic_text_fallback` (NOT "basic_text")
- PIPELINE_STAGES: ingest, route, parse, normalize, extract, compile, patch, lint, harness, gate


## [2026-04-15 10:30] - US-012
- Verified that formula-heavy signals are already fully implemented in route scoring
- router.py `_score_route()` handles `is_formula_heavy` signal at line 207-208, adding score points when `table_formula_heavy=True` routes encounter formula-heavy documents
- `_build_decision()` records `is_formula_heavy` in `matched_signals` for audit logging
- Tests in `tests/test_formula_heavy_us016.py` cover all acceptance criteria
- **Changed files:** scripts/ralph/prd.json (set passes: true)
- **Future iteration learnings:**
  - US-012 through US-022 features may have been implemented in a previous PRD cycle — always check existing code and tests first
  - Test files follow naming pattern `tests/test_<feature>_usXXX.py`
---

## [2026-04-15 14:00] - US-001
- Verified all acceptance criteria are met by existing code
- `docos run <source_path>` command registered in `docos/cli/main.py`
- `PipelineRunner.run()` invokes all 10 stages: ingest → route → parse → normalize → extract → compile → patch → lint → harness → gate
- Failed stage exits with SystemExit(1) and names the failing stage
- Existing test file `tests/test_pipeline_runner.py` covers all scenarios
- Added `tests/test_unified_run_us001.py` for traceability
- **Changed files:** tests/test_unified_run_us001.py (new), scripts/ralph/prd.json (passes: true)
- **Future iteration learnings:**
  - US-001 infrastructure already complete from previous cycle
---

## [2026-04-15 14:05] - US-002
- Verified all acceptance criteria met by existing code
- RunStore.create() generates deterministic run_id and persists to `<base>/manifests/<run_id>.json`
- RunManifest stores run_id, source_id, source_file_path, started_at, finished_at
- RunStore.get() reloads manifest by run_id in fresh process
- Tests in `tests/test_run_manifest_us002.py` cover all scenarios (7 tests)
- **Changed files:** scripts/ralph/prd.json (passes: true)
- **Future iteration learnings:**
  - RunStore uses `<base>/manifests/<run_id>.json` layout
  - run_id format: `run_<hash_prefix>_<time_hex>`
---

## [2026-04-15 14:10] - US-003
- Verified all acceptance criteria met by existing code
- PipelineStage model has name, status, started_at, completed_at, error_detail, duration_seconds
- PipelineRunner marks each stage RUNNING/COMPLETED/FAILED with timestamps
- Failed run: stages before failure stay COMPLETED, failed stage is FAILED with error_detail, later stages remain PENDING
- Tests in `tests/test_artifact_persistence_us003.py` cover all scenarios including failure case (10 tests)
- **Changed files:** scripts/ralph/prd.json (passes: true)
---

## [2026-04-15 16:00] - US-023
- Created test file `tests/test_eval_real_artifacts_us023.py` with 11 tests covering all acceptance criteria
- Eval (HarnessRunner) already reads persisted DocIR, claims, entities, patch, and previous_report from stores
- IRStore, KnowledgeStore, PatchStore, ReportStore all support save/get round-trip for real artifacts
- Missing DocIR produces blocking eval result (parse_quality.passed=False, release_decision="review_required")
- Bad fixtures (low confidence, high-severity warnings, unsupported claims) produce blocking eval results
- No empty placeholder inputs: when all artifacts are provided, metrics reflect real data
- **Changed files:** tests/test_eval_real_artifacts_us023.py (new), scripts/ralph/prd.json (passes: true)
- **Future iteration learnings:**
  - HarnessRunner.run() already accepts previous_report parameter for regression checking
  - ClaimRecord.model_construct() bypasses pydantic validation — useful for injecting bad data in tests
  - DocIR.confidence is mutable after construction, allowing test manipulation
---

## [2026-04-15 14:20] - US-004
- Ingest persists: raw source copy (raw/<source_id>/original.pdf), source registry record (registry/records/<source_id>.json), content hash (raw/<source_id>/metadata.json)
- Route persists: route_decision.json (routes/<run_id>.json), route log with signals (route_logs/route_<source_id>.json), scoring details (route_scores in route log)
- RunManifest links to route_artifact_path, source_id, source_file_path
- Created test file `tests/test_ingest_route_artifacts_us004.py` with 8 tests
- **Changed files:** tests/test_ingest_route_artifacts_us004.py (new), scripts/ralph/prd.json (passes: true)
---

## [2026-04-15 14:30] - US-005
- Parse persists: parser attempt logs (debug/<source_id>/<run_id>/<parser>/parse_log.json), parse results (DocIR via IRStore), debug assets (assets_index.json, raw_output.json)
- Normalize persists: canonical DocIR (overwrites IRStore with repaired version), repair logs via RepairLog model
- RunManifest links: ir_artifact_path, debug_artifact_path
- Created test file `tests/test_parse_normalize_artifacts_us005.py` with 7 tests
- **Changed files:** tests/test_parse_normalize_artifacts_us005.py (new), scripts/ralph/prd.json (passes: true)
---

## [2026-04-15 14:40] - US-006
- Extract persists: entities.json, claims.json, relations.json in knowledge/<run_id>/
- Compile persists: wiki page state in wiki_state/*.json
- Patch persists: patch artifacts via PatchStore
- Lint persists: lint findings in lint_results/<run_id>.json
- Eval (harness) persists: report in reports/<run_id>.json
- Gate persists: gate_decision in RunManifest (passed/blocked)
- RunManifest links to knowledge_artifact_path, lint_artifact_path, report_artifact_path, patch_artifact_path
- Created test file `tests/test_downstream_artifacts_us006.py` with 11 tests
- **Changed files:** tests/test_downstream_artifacts_us006.py (new), scripts/ralph/prd.json (passes: true)
---

## [2026-04-15 14:50] - US-007
- `docos report <run_id>` reconstructs full run state from persisted stores
- Report shows: source registry info, route result, parser chain, fallback state, DocIR summary, knowledge summary, patch state, lint summary, harness result, release decision, review status
- Failed run report includes failing stage and error details from RunManifest
- No placeholder strings: all output generated from stores and manifest data
- Created test file `tests/test_report_reconstruct_us007.py` with 12 tests
- **Changed files:** tests/test_report_reconstruct_us007.py (new), scripts/ralph/prd.json (passes: true)
---

## [2026-04-15 15:00] - US-008
- Added `docos rerun <source_id>` CLI command to create new run from existing source
- `docos report <run_id>` inspects saved run without rerunning (already existed)
- Failed run preserves completed artifact references (route artifact, source registry record)
- Created test file `tests/test_rerun_report_us008.py` with 7 tests
- **Changed files:** docos/cli/main.py (added rerun command), tests/test_rerun_report_us008.py (new), scripts/ralph/prd.json (passes: true)
---

## [2026-04-15 15:10] - US-009
- ParserRouter.validate_config() checks all primary_parser and fallback_parsers against ParserRegistry
- Unknown parser names return as unresolved list (structured validation failure)
- Tests cover valid config, invalid primary, invalid fallback, mixed, and registry resolution
- Created test file `tests/test_route_config_validation_us009.py` with 6 tests
- **Changed files:** tests/test_route_config_validation_us009.py (new), scripts/ralph/prd.json (passes: true)
---

## [2026-04-15 10:45] - US-013
- Added RouteAuditEntry dataclass for per-route score tracking
- Added route_scores field to RouteDecision for storing all candidate route scores, accepted/rejected status, and rejection reasons
- Updated _match_route() to return audit trail alongside the best route
- Updated _build_decision() to accept and store audit entries
- Updated RouteLogEntry.to_dict() to include route_scores in JSON output
- max_pages is documented in code as soft score (not hard filter)
- Test file: tests/test_max_pages_audit_us013.py (9 tests)
- **Changed files:** docos/pipeline/router.py, tests/test_max_pages_audit_us013.py, scripts/ralph/prd.json
- **Future iteration learnings:**
  - RouteDecision now has a route_scores list of RouteAuditEntry objects
  - _match_route returns tuple (ParserRoute | None, list[RouteAuditEntry])
---

## [2026-04-15 10:55] - US-014
- Verified parse CLI command already loads route decisions before parser execution
- CLI parse command: route → registry → orchestrator flow (no direct parser instantiation)
- PipelineRunner._stage_parse() receives route_decision from _stage_route()
- Tests in test_parse_route_us005.py verify the integration
- **Changed files:** scripts/ralph/prd.json (set passes: true)
- **Future iteration learnings:**
  - Parse CLI always runs route first (does not check for existing route artifacts)
  - Orchestrator.execute() takes a RouteDecision parameter directly
---

## [2026-04-15 11:00] - US-015
- Verified parser resolution through ParserRegistry is fully implemented
- PipelineOrchestrator.execute() resolves primary and fallback parsers via registry.get()
- Missing parser → clear structured error (parser_unavailable list + failure_reason)
- Tests in test_parser_resolution_us006.py cover primary, fallback, unknown, and fallback-success
- **Changed files:** scripts/ralph/prd.json (set passes: true)
---

## [2026-04-15 11:05] - US-016
- Verified orchestrator chain execution fully implemented
- Primary parser runs first, stops on success
- Fallback chain invoked on primary failure, in order
- PipelineRunResult records fallback_used, fallback_parser, final_parser, attempts
- Tests in test_orchestrator_chain_us007.py (9 tests) cover all acceptance criteria
- **Changed files:** scripts/ralph/prd.json (set passes: true)
---

## [2026-04-15 16:30] - US-024
- Added release_reasoning and gate_blockers fields to HarnessReport (dataclass)
- Added gate_blockers and release_reasoning fields to RunManifest (pydantic model)
- Updated PipelineRunner._stage_gate() to persist gate decision, blockers, review_status
- Added _persist_gate_decision() method to write structured gate JSON artifact
- Updated ReportStore serialization to include new fields with backward-compatible loading
- Test file: tests/test_gate_persist_us024.py (11 tests)
- **Changed files:** docos/harness/runner.py, docos/models/run.py, docos/pipeline/runner.py, docos/artifact_stores.py, tests/test_gate_persist_us024.py, scripts/ralph/prd.json
- **Future iteration learnings:**
  - HarnessReport.release_reasoning is auto-populated in compute_overall() based on which sections failed
  - RunManifest now has gate_blockers and release_reasoning lists alongside gate_decision
  - Gate decision artifacts stored in <base>/gate_decisions/<run_id>.json
---

## [2026-04-15 11:15] - US-017
- Verified CREATE_PAGE patches already implemented in CompiledPage.compute_patch()
- compute_patch returns CREATE_PAGE when existing_body is None (new page)
- Patch stores target page path, source_id, run_id, and content summary
- Tests in test_patch_types_us018.py cover create, update, and delete patch generation
- **Changed files:** scripts/ralph/prd.json (set passes: true)
---

## [2026-04-15 11:20] - US-018
- Verified UPDATE_PAGE and DELETE_PAGE patches already implemented
- UPDATE_PAGE when existing_body has changed content (risk_score=0.3)
- DELETE_PAGE when deleted=True flag is set (risk_score=0.5)
- Tests in test_patch_types_us018.py cover both update and delete cases
- **Changed files:** scripts/ralph/prd.json (set passes: true)
---

## [2026-04-15 11:25] - US-019
- Verified patch IDs are deterministic using SHA-256 hash
- compute_patch in compiler.py uses hashlib.sha256(body.encode()).hexdigest()[:12]
- Same content always produces same patch_id regardless of run_id or source_id
- Tests in test_patch_deterministic_us019.py (5 tests) verify determinism
- **Changed files:** scripts/ralph/prd.json (set passes: true)
---

## [2026-04-15 17:00] - US-025
- Created test file tests/test_lint_harness_joint_us025.py (9 tests)
- ReleaseGate already implements all joint gate logic: P0 blocks, missing harness blocks, fallback blocks
- Verified lint + harness jointly control auto-merge through real WikiLinter and ReleaseGate integration
- Tests cover blocked cases (P0, missing harness, both fail) and review-required cases (fallback low confidence)
- Report and manifest include final gate decision and all blocking reasons
- **Changed files:** tests/test_lint_harness_joint_us025.py (new), scripts/ralph/prd.json
---

## [2026-04-15 17:30] - US-026
- Added OverrideAuditRecord dataclass to docos/lint/checker.py with reviewer, reason, overridden_checks, timestamp, original_gate_decision
- Added ReleaseGate.override() method that creates audit records with required reason validation
- Added override_reviewer, override_reason, override_timestamp, overridden_checks fields to RunManifest
- Override reason is required (empty/whitespace raises ValueError)
- Test file: tests/test_override_audit_us026.py (8 tests)
- Also fixed: BlastRadius import in wiki/compiler.py (from concurrent agent changes)
- Also fixed: lint_summary always includes "total" count so empty findings still serialize as truthy
- Also fixed: test_patch_types_us018.py delete risk assertion to match updated risk calculation
- **Changed files:** docos/lint/checker.py, docos/models/run.py, tests/test_override_audit_us026.py, docos/wiki/compiler.py, docos/pipeline/runner.py, tests/test_patch_types_us018.py, scripts/ralph/prd.json
- **Future iteration learnings:**
  - Empty dict `{}` is falsy in Python — when persisting summary fields, always include at least one key like `{"total": 0}`
  - OverrideAuditRecord is a dataclass in checker.py, not a pydantic model
  - Other agents may modify risk_score calculations — use range assertions instead of exact values
---

## [2026-04-15 11:45] - US-020
- Implemented real blast radius computation in CompiledPage.compute_patch()
- Blast radius now derived from: pages (always 1), claims (from frontmatter.related_claims), links (from frontmatter.related_entities)
- Risk score computed from real diff data using difflib.unified_diff for line-level change analysis
- Delete risk: 0.5 base + scaled by claims/pages count
- Create risk: scales with content size
- Update risk: proportional to change ratio (changed_lines / total_lines)
- Created tests/test_blast_radius_risk_us020.py (12 tests)
- Updated test_patch_types_us018.py to use range assertions instead of fixed values
- Fixed test_raw_fixtures_us026.py for 4 fixtures (was expecting 3)
- **Changed files:** docos/wiki/compiler.py, tests/test_blast_radius_risk_us020.py, tests/test_patch_types_us018.py, tests/test_raw_fixtures_us026.py, scripts/ralph/prd.json
- **Future iteration learnings:**
  - Changing risk_score computation breaks tests with hardcoded expected values
  - build_fixtures.py now has 4 fixtures (simple_text, dual_column_or_formula, ocr_like, table_formula)
  - Other agents may be working concurrently — check for untracked test files that could break collection
---

## [2026-04-15 11:55] - US-021
- Verified patch apply and merge lifecycle fully implemented
- PatchService exposes apply_patch(), auto_merge(), approve_merge(), rollback(), reject()
- MergeStatus: PENDING, AUTO_MERGED, APPROVED, REJECTED, ROLLED_BACK
- Merge saves pre_merge_snapshot from WikiStore before applying
- Tests in test_patch_lifecycle_us020.py (9 tests) cover full lifecycle
- **Changed files:** scripts/ralph/prd.json (set passes: true)
---

## [2026-04-15 12:05] - US-022
- Verified patch rollback and review linkage fully implemented
- Rollback restores pre-merge wiki state via pre_merge_snapshot
- REJECTED and ROLLED_BACK states in MergeStatus enum
- ReviewItem links to patch via target_object_id (patch_id)
- ReviewAction has linked_patch_id field
- ReviewQueue resolves items and loads associated patch artifacts
- Tests: test_patch_rollback_us021.py (7 tests), test_review_patch_link_us022.py (5 tests), test_review_patch_us022.py (6 tests)
- **Changed files:** scripts/ralph/prd.json (set passes: true)
---

## [2026-04-15 18:00] - US-027
- Added table_formula.pdf fixture builder in tests/fixtures/build_fixtures.py
- Fixture covers table-heavy and formula-heavy content with structured benchmark data and math expressions
- Now have 4 fixture categories: simple_text, dual_column_or_formula, ocr_like, table_formula
- Test file: tests/test_e2e_source_fixtures_us027.py (15 tests)
- All fixtures are valid PDFs that parse successfully through SignalExtractor
- Also restored OverrideAuditRecord and override fields in checker.py and run.py (concurrent agent conflict)
- **Changed files:** tests/fixtures/build_fixtures.py, tests/test_e2e_source_fixtures_us027.py, docos/lint/checker.py, docos/models/run.py
- **Future iteration learnings:**
  - Other agents may overwrite checker.py and run.py changes — need to re-add OverrideAuditRecord and override fields
  - Four fixture categories now available: simple_text, dual_column_or_formula, ocr_like, table_formula
  - test_rerun_report_us008.py failures are from missing CLI rerun command (not my story)
---

## [2026-04-15 15:20] - US-010
- Selected SHRINK-TO-IMPLEMENTED strategy: router.yaml only references implemented parsers
- Updated configs/router.yaml: replaced pymupdf/pdfplumber/marker/paddleocr/tesseract with stdlib_pdf and basic_text_fallback
- Complex routes commented out as placeholders with install guidance
- Strategy documented in router.yaml header
- Created test file `tests/test_parser_alignment_us010.py` with 4 tests
- **Changed files:** configs/router.yaml, tests/test_parser_alignment_us010.py (new), scripts/ralph/prd.json (passes: true)
- **Future iteration learnings:**
  - SHRINK-TO-IMPLEMENTED is the safer strategy — avoids runtime warnings and silent skips
  - When adding new parser adapters: implement, register in PipelineRunner, uncomment route in router.yaml
---
