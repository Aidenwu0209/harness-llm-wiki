## Codebase Patterns
- This repository is Python-first; use `python -m mypy docos/` for type checking and `python -m pytest tests/ -q` for automated verification. Use `pip install -i https://pypi.tuna.tsinghua.edu.cn/simple` for network-reliable installs.
- Batch verification entrypoints live in `scripts/quick_verify_papers.py` and `scripts/download_and_verify_papers.py`.
- Pipeline quality signals already flow through `docos/pipeline/runner.py`, `docos/run_store.py`, `docos/artifact_stores.py`, `docos/review/queue.py`, and `docos/wiki/compiler.py`; prefer reusing those artifacts instead of inventing parallel state.
- Focus test coverage on small targeted files under `tests/` and reuse fixture helpers from `tests/fixtures/build_fixtures.py` when batch or parser fixtures are needed.
- `_export_wiki_pages()` returns a dict `{"exported": [...], "filtered_empty_slug": int}` — callers must access `["exported"]` for the path list and `["filtered_empty_slug"]` for the rejected count.
- Empty-slug rejection happens at two layers: compile stage (`_stage_compile` via `_is_valid_page_path`) and export stage (`_export_wiki_pages`). Both contribute to `dropped_empty_slug_count` in per-paper results.
- `PipelineResult.gate_passed` is `bool | None` (None = gate stage never reached); `RunManifest.review_status` can be `None`, `"pending"`, `"auto_merged"`, `"approved"`, `"rejected"`, `"changes_requested"`.
- Verdict tier classification lives in `_classify_verdict()` in `scripts/quick_verify_papers.py`; downstream stories should reuse this function rather than re-implementing gate/review logic.
- Aggregate quality metrics (`gate_pass_rate`, `lint_blocker_count`, `pending_review_count`) are computed in `run_batch()` and added to the `totals` dict; `pending_review_count` was already present from US-003.

## 2026-04-18 00:14 - US-001
- Added `_classify_verdict()` function to `scripts/quick_verify_papers.py` that assigns each paper one of three verdict tiers: `pipeline_runnable`, `quality_blocked`, or `usable_wiki_ready`
- Classification logic: completed + gate_passed=True + wiki exported → usable_wiki_ready; completed + (gate_passed=False or review_status=pending) → quality_blocked; everything else → pipeline_runnable
- Added `verdict` field to each per-paper result (in both `_build_file_result` and `_build_script_failure` paths)
- Added verdict tier counts (`pipeline_runnable_count`, `quality_blocked_count`, `usable_wiki_ready_count`) to batch totals in `summary.json`
- Updated `_write_summary_md` to show "Verdict Tiers" section and added "Verdict" column to per-paper table
- Added 3 new tests verifying verdict field presence, tier counts in summary, and per-file result.json verdict
- **Changed files:** `scripts/quick_verify_papers.py`, `tests/test_quick_verify_papers.py`
- **未来迭代的学习：**
  - Verdict tier is computed post-construction via `_classify_verdict(item)` called on each result dict
  - The `_build_verdict` top-level function still uses old success_count/wiki_count logic — that is US-007's scope to rewrite
  - `mypy` is now installed in conda env via Tsinghua mirror (`pip install mypy -i https://pypi.tuna.tsinghua.edu.cn/simple`); use this mirror for all future pip installs in China network
---

## 2026-04-18 00:51 - US-001 (fix)
- Fixed US-001 validation failure: installed mypy into conda environment using Tsinghua PyPI mirror
- All 6 ACs now pass: mypy (Success: no issues found in 45 source files), pytest (1039 passed), functional verdict tiers verified
- No code changes needed — the original US-001 implementation was correct; only the environment was missing mypy
- **Changed files:** `scripts/ralph/prd.json` (passes: true)
- **未来迭代的学习：**
  - Use `pip install -i https://pypi.tuna.tsinghua.edu.cn/simple` for reliable package installs in China network
  - `python -m mypy docos/` is the canonical typecheck command (not `mypy docos/`)
---

## 2026-04-18 00:58 - US-002
- Verified that US-001's `_classify_verdict()` already correctly classifies gate-blocked papers as `quality_blocked`
- Added 5 targeted tests for US-002 ACs: gate.passed=false → quality_blocked, blocked even with wiki pages, excluded from usable_wiki_ready tally, gate info preserved in per-paper payload, gate.passed=None edge case
- All 6 ACs pass: mypy clean, 1044 tests pass
- **Changed files:** `tests/test_quick_verify_papers.py`
- **未来迭代的学习：**
  - US-001's `_classify_verdict()` already covers US-002 and US-003 classification logic; later stories should focus on additional signals (knowledge_sparse, wiki_sparse) rather than reworking verdict classification
  - `_classify_verdict` is a pure function that can be imported directly in tests via `sys.path.insert(0, str(REPO_ROOT))` then `from scripts.quick_verify_papers import _classify_verdict`
  - `gate.passed` is `bool | None`; `None` means gate stage was never reached and should NOT be treated as blocked
---

## 2026-04-18 01:07 - US-003
- Verified `_classify_verdict()` already correctly classifies `review_status=pending` as `quality_blocked` (not `usable_wiki_ready`)
- Added `pending_review_count` to batch summary totals (counts papers with `review_status == "pending"`)
- Added "Pending review" line to markdown Verdict Tiers section
- Added "Review Status" column to markdown per-paper table showing original `review_status` value
- Added 4 targeted tests for US-003 ACs: pending not usable_wiki_ready, separate counter in summary, verdict+review_status together, markdown column
- All ACs pass: mypy clean, 1048 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `tests/test_quick_verify_papers.py`
- **未来迭代的学习：**
  - `_classify_verdict()` handles both `gate_passed is False` and `review_status == "pending"` in one check — both map to `quality_blocked`
  - `pending_review_count` is a separate counter from `quality_blocked_count`, allowing downstream consumers to see the breakdown
---
## 2026-04-18 01:14 - US-004
- Added `_is_knowledge_sparse()` helper that checks `entities=0, claims=0, relations=0`
- Added `knowledge_sparse` boolean field to per-paper results (computed alongside `verdict`)
- Added `knowledge_sparse_count` to batch summary totals in `summary.json`
- Added "Knowledge sparse" line to markdown Verdict Tiers section
- Added 6 tests: all-zero → True, non-zero entities → False, counter in summary, coexists with verdict, per-paper result.json field, markdown line
- All ACs pass: mypy clean, 1054 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `tests/test_quick_verify_papers.py`
- **未来迭代的学习：**
  - `_is_knowledge_sparse()` is a pure function like `_classify_verdict()`, computed post-construction on the result dict
  - Both `verdict` and `knowledge_sparse` are added to `file_result` in the same post-processing block in `run_batch()`, making it easy to add more signals later
  - Knowledge-sparse is independent of the verdict tier — a paper can be `usable_wiki_ready` AND `knowledge_sparse` if it exported wiki pages but had zero knowledge extraction (edge case from old pipeline runs)
---

## 2026-04-18 01:22 - US-005
- Added `_is_wiki_sparse()` helper that checks exported wiki page paths for entity/concept directory components
- Added `wiki_sparse` boolean field to per-paper results (computed alongside `verdict` and `knowledge_sparse`)
- Added `wiki_sparse_count` to batch summary totals in `summary.json`
- Added "Wiki sparse" line to markdown Verdict Tiers section
- Added 7 tests: source-only → True, entity pages → False, concept pages → False, no pages → False, counter in summary, per-paper result.json field, markdown line
- All ACs pass: mypy clean, 1061 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `tests/test_quick_verify_papers.py`
- **未来迭代的学习：**
  - Wiki page types are determined by directory components: `sources/`, `entities/`, `concepts/` — checked via `Path(page_path).parts`
  - `_is_wiki_sparse()` returns False when no pages exist (signal only meaningful when pages are exported)
  - The three post-processing signals (`verdict`, `knowledge_sparse`, `wiki_sparse`) are all computed in the same block in `run_batch()`, making it easy to add more signals
---

## 2026-04-18 01:29 - US-006
- Added explicit coverage counters (`manifest_total`, `selected_paper_count`, `downloaded_paper_count`, `verified_paper_count`) to both `quick_verify_papers.py` and `download_and_verify_papers.py` summary.json totals
- Added "Sample Coverage" section to markdown output in both scripts, clearly showing manifest→selected→downloaded→verified funnel
- For quick_verify: `manifest_total` = all PDFs discovered, `downloaded_paper_count` = selected (PDFs are local)
- For download_and_verify: `manifest_total` = manifest YAML total, `downloaded_paper_count` = actual downloads
- Added 6 tests: 3 for quick_verify (counters in JSON, markdown section, partial batch subset), 3 for download_and_verify (counters in JSON, partial batch, markdown section)
- All ACs pass: mypy clean, 1067 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `scripts/download_and_verify_papers.py`, `tests/test_quick_verify_papers.py`, `tests/test_download_and_verify_papers.py`
- **未来迭代的学习：**
  - Coverage counters are additive to existing totals dict, not replacing the older `pdfs_discovered`/`pdfs_selected`/`pdfs_processed` names — both coexist for backward compatibility
  - `download_and_verify_papers.py` reuses quick_verify's `run_batch()` via import, so any counters added to quick_verify automatically appear in the inner verify payload; the outer payload needs its own explicit copies
  - Markdown "Sample Coverage" section is placed before "Run Outcomes" to make coverage visible first
---

## 2026-04-18 01:38 - US-007
- Rewrote `_build_verdict()` to derive headline/answer from verdict-tier totals (`usable_wiki_ready`, `quality_blocked`, `pipeline_runnable`) instead of raw `success_count` and `wiki_count`
- Added `manifest_total` and `verified_paper_count` keyword parameters to `_build_verdict()` for coverage-aware verdict text
- When coverage is partial (verified < manifest), the answer explicitly states the sample limitation with counts
- Added `quality_blocked` status tier: all-blocked runs produce a distinct headline mentioning quality blocking
- Updated `run_batch()` call site to pass `discovered_count` and `len(results)` as coverage parameters
- Added 6 targeted tests: verdict-tier derivation, blocked-no-fully-ready, partial-coverage-note, all-quality-blocked, no-results-edge, full-coverage-no-warning
- All ACs pass: mypy clean, 1073 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `tests/test_quick_verify_papers.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - `_build_verdict()` in `download_and_verify_papers.py` is a separate function with its own signature — it wraps quick_verify's verdict and does NOT need to change when quick_verify's `_build_verdict` changes
  - The `download_and_verify` version passes through quick_verify's verdict via `verify_payload["verdict"]`, so any quality-aware improvements to quick_verify's verdict automatically flow downstream
  - New `_build_verdict` signature uses keyword-only args (`*`) for coverage params, maintaining backward compatibility with callers that only pass `results`
---

## 2026-04-18 01:44 - US-008
- Added "Gate" column to markdown per-paper table showing gate decision (falls back to passed/blocked display when decision is None)
- Verified per-paper JSON already includes `gate` (with `decision`), `review_status`, and `verdict` together (AC1 was already satisfied by prior stories)
- Added 5 tests: per-paper JSON has gate+review+verdict, markdown Gate column header, all three columns side by side, blocked paper inspectable from summary, individual result.json has all three signals
- All ACs pass: mypy clean, 1078 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `tests/test_quick_verify_papers.py`
- **未来迭代的学习：**
  - The per-paper result dict already has `gate`, `review_status`, and `verdict` at the same top level — no structural changes needed for JSON ACs
  - Gate display in markdown uses `gate.decision` when available, falls back to "passed"/"blocked" derived from `gate.passed` boolean, then "-" as final fallback
  - When adding table columns, the separator row must also be updated to match column count
---

## 2026-04-18 01:51 - US-009
- Added `gate_pass_rate` (float 0–1 or None when no papers reached gate stage), `lint_blocker_count`, and consolidated `pending_review_count` as aggregate quality metrics in `summary.json` totals
- Added `_fmt_rate()` helper to format gate pass rate as human-readable percentage (e.g., "50%", "N/A")
- Added "Quality Metrics" section to markdown summary output rendering gate pass rate, pending review count, and lint blocker count
- Added 6 targeted tests: quality metrics in JSON, markdown section, gate pass rate computation, None when no gate evaluated, lint blocker counting, _fmt_rate helper
- All ACs pass: mypy clean, 1084 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `tests/test_quick_verify_papers.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - `gate_pass_rate` is computed from papers where `gate.passed is not None` (i.e., papers that actually reached the gate stage), not from all papers
  - `pending_review_count` was already tracked from US-003; US-009 reuses it in the new Quality Metrics section rather than creating a duplicate
  - `_fmt_rate()` uses Python's format spec `:.0%` which rounds to nearest integer percentage
---
## 2026-04-18 02:00 - US-010
- Added `page_buckets` dict to per-paper results with `generated_candidate_pages`, `gate_passed_pages`, `final_vault_ready_pages`
- Added same three fields as aggregate totals in `summary.json` totals
- Added "Generated candidate pages", "Gate-passed pages", "Final vault-ready pages" lines to markdown Run Outcomes section
- Per-paper `page_buckets` is computed inside the per-paper loop (before `result.json` write), so individual result files include it
- Aggregate totals are derived by summing per-paper `page_buckets` values
- Added 6 tests: summary JSON buckets, per-paper result.json buckets, blocked paper excluded from vault_ready, markdown lines, per-paper bucket reflects verdict, zero-pages edge case
- All ACs pass: mypy clean, 1090 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `tests/test_quick_verify_papers.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - Per-paper post-processing fields (`verdict`, `knowledge_sparse`, `wiki_sparse`, `page_buckets`) must be added BEFORE `result.json` write to be included in individual run files
  - Aggregate totals can then simply sum the per-paper values rather than recomputing from raw data
  - `page_buckets` uses the same gate.passed and verdict signals already established by prior stories — no new classification logic needed
---
## 2026-04-18 02:13 - US-011
- Rewrote `_build_verdict()` in `scripts/download_and_verify_papers.py` to be independently quality-aware (no longer a passthrough of quick-verify verdict)
- Added quality summary fields to download-and-verify totals: `download_success_rate`, `verify_success_rate`, `gate_pass_rate`, `pending_review_count`, `usable_wiki_ready_count`, `quality_blocked_count`
- Added `_fmt_rate()` helper to download_and_verify for formatting rates in markdown
- Added "Quality Metrics" section to markdown summary output with all six quality fields
- Verdict composition now considers usable_wiki_ready_count, quality_blocked_count, pending_review_count from quick-verify totals
- Added 6 tests: JSON quality fields, verdict not passthrough, both root+session preserve fields, markdown section, partial batch, unit test for _build_verdict
- All ACs pass: mypy clean, 1096 tests pass
- **Changed files:** `scripts/download_and_verify_papers.py`, `tests/test_download_and_verify_papers.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - `_build_verdict()` in download_and_verify is now fully independent — it extracts quality signals from `verify_payload["totals"]` and composes its own verdict status/headline/answer
  - Quality fields are computed from `verify_totals` which comes from quick_verify's `run_batch()` — all US-009 metrics automatically flow into download-and-verify
  - Both root and session summary files share the same payload dict, so adding fields to the payload automatically updates both outputs
  - `_fmt_rate()` helper is duplicated between quick_verify and download_and_verify — they could share it, but keeping them separate avoids coupling between the two scripts
---
## 2026-04-18 15:30 - US-012
- Verified `docos/slugify.py` already implements a complete deterministic ASCII-safe slug sanitizer
- Verified `docos/wiki/compiler.py` delegates all slug generation through `_slug()` → `docos.slugify.slugify()` (single canonical path)
- Added 20 targeted tests in `tests/test_slugify.py` covering all 3 ACs: canonical sanitizer delegation, ASCII-safe output (no control chars, only `[a-z0-9-]`), and determinism (same input → same output)
- All ACs pass: mypy clean (46 source files), 1116 tests pass
- **Changed files:** `tests/test_slugify.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - `docos/slugify.py` is the single canonical slug module — all wiki page filenames must go through it
  - `_slug()` in `compiler.py` is a thin wrapper that imports and delegates to `docos.slugify.slugify`
  - Control characters are stripped (not replaced with hyphens) — "hello\x00world" → "helloworld", not "hello-world"
  - Empty slug fallback returns "untitled" — important for US-013 (rejecting empty slugs)
  - `_sanitize_key()` in `artifact_stores.py` is a separate function for store keys (not filenames), uses simpler logic
---

## 2026-04-18 07:26 - US-013
- Changed `_export_wiki_pages()` return type from `list[str]` to `dict[str, Any]` with `{"exported": [...], "filtered_empty_slug": int}` to expose empty-slug rejection count
- Added `dropped_empty_slug_count` to per-paper results (sum of manifest compile-stage drops + export-stage filter count)
- Added `dropped_empty_slug_count` to batch summary totals in `summary.json`
- Added "Dropped empty slug" line to markdown Verdict Tiers section
- Added source page slug validation in `_stage_compile` (was missing — only entity and concept pages were validated before)
- Extended test suite from 8 to 15 tests: added mixed empty/valid export test, per-paper dropped count, batch summary totals, markdown output lines, filesystem safety check
- All ACs pass: mypy clean (46 source files), 1131 tests pass
- **Changed files:** `scripts/quick_verify_papers.py`, `docos/pipeline/runner.py`, `tests/test_empty_slug_rejection_us013.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - `_export_wiki_pages` now returns a dict, not a list — any new callers must access `["exported"]` for the path list
  - Empty-slug rejection has two layers: compile stage in runner.py and export stage in quick_verify — both counted separately and summed in per-paper results
  - Source pages are now also validated for empty slugs in `_stage_compile`, so ALL page types (source, entity, concept) go through the same `_is_valid_page_path` gate
---

## 2026-04-18 07:44 - US-014
- Added `sanitize_title()` and `is_readable_title()` to `docos/slugify.py` for page title hygiene
- `sanitize_title()` removes control chars (\x00-\x08, \x0b, \x0c, \x0e-\x1f, \x7f-\x9f, \ufffd), private-use codepoints (\ue000-\uf8ff), and collapses whitespace
- `is_readable_title()` checks that a title has >= 30% alphanumeric/readable characters (configurable threshold)
- Applied `_clean_title()` wrapper in all `WikiCompiler.compile_*()` methods so both frontmatter title and body heading use sanitized titles
- Added readability gate in `runner.py._stage_compile()` — entity and concept pages with unreadable names are skipped before compilation
- Added `dropped_unreadable_title_count` field to `RunManifest` model and persist it from compile stage
- Added 31 tests covering: control char removal, binary garbage filtering, CJK preservation, readability thresholds, compiler integration, runner skip behavior
- All ACs pass: mypy clean (46 source files), 1162 tests pass
- **Changed files:** `docos/slugify.py`, `docos/wiki/compiler.py`, `docos/pipeline/runner.py`, `docos/models/run.py`, `tests/test_title_sanitization_us014.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - Title sanitization is applied via `_clean_title()` helper in compiler.py — same pattern as `_slug()` for filenames
  - The readability check happens in the runner BEFORE compilation (more efficient than compiling then discarding)
  - `dropped_unreadable_title_count` is tracked separately from `dropped_empty_slug_count` in the manifest
  - `sanitize_title()` preserves CJK and normal punctuation — only strips control chars, private-use, and replacement characters
  - Entity page tests use `_make_entity()` mock helper; concept pages take `concept_name` string directly
---

## 2026-04-18 08:02 - US-015
- Verified US-014's implementation already covers all US-015 ACs: `runner.py._stage_compile()` skips entity and concept candidates that fail `is_readable_title()` check
- Added 21 targeted tests in `tests/test_unreadable_compile_skip_us015.py` covering all 3 ACs: compile-time skip gate, no output for unreadable candidates, deterministic behavior
- Tests verify: garbage entity/concept names detected, readable names pass, CJK names readable, sanitized-empty names unreadable, compiler handles garbage without crash, determinism across 100 repeated calls
- All ACs pass: mypy clean (46 source files), 1183 tests pass
- **Changed files:** `tests/test_unreadable_compile_skip_us015.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - US-014 already implemented the runner-side readability gate — US-015's scope was primarily test coverage
  - The `_stage_compile()` method applies `is_readable_title()` to both entity names and concept names before calling `compile_entity_page()` / `compile_concept_page()` — the compiler still compiles but the runner drops the result
  - Pure garbage input sanitizes to empty string (`""`) — compiler produces frontmatter with empty title, but runner's `is_readable_title("")` returns `False`, so the page is never persisted
---

## 2026-04-18 08:15 - US-016
- Added `dropped_unreadable_entity_count` and `dropped_unreadable_concept_count` fields to `RunManifest` model
- Split `_dropped_unreadable_title` tracking in `runner.py._stage_compile()` into entity-level and concept-level counters (both still contribute to the total `dropped_unreadable_title_count`)
- Surfaced `dropped_unreadable_title_count`, `dropped_unreadable_entity_count`, `dropped_unreadable_concept_count` in per-paper results and batch summary totals in `quick_verify_papers.py`
- Added four dropped-count lines to markdown Verdict Tiers section
- Ensured all filtered-count fields are present even when zero (including script-failure results via `setdefault`)
- Added 6 targeted tests: summary JSON fields, per-paper result.json fields, markdown lines, fields-present-when-zero, script-failure-includes-zero, aggregate-totals-match-sum
- All ACs pass: mypy clean (46 source files), 1189 tests pass
- **Changed files:** `docos/models/run.py`, `docos/pipeline/runner.py`, `scripts/quick_verify_papers.py`, `tests/test_dropped_candidate_stats_us016.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - `dropped_unreadable_title_count` is the combined total; `dropped_unreadable_entity_count` and `dropped_unreadable_concept_count` are the breakdown — both sum to equal the total
  - `setdefault` pattern ensures script-failure results also get zero-valued dropped-count fields without special-casing the `_build_script_failure` constructor
  - All three dropped-count dimensions (empty_slug, unreadable_title, entity/concept split) are additive in the manifest and quick_verify follows the same pattern as existing US-013 empty-slug tracking
---

## 2026-04-18 08:41 - US-017
- Added `_sanitize_heading_title_text()` method to `GlobalRepair` in `normalizer.py` that applies `sanitize_title()` to `text_plain` and `text_md` of HEADING and TITLE blocks during normalization (stage 4)
- This ensures downstream extraction (stage 5) consumes cleaned candidates — control chars, private-use codepoints, and whitespace are sanitized BEFORE entities/claims are created
- The sanitization step runs BEFORE all other repairs (heading hierarchy, repeated header/footer removal, etc.) so downstream repairs also see clean text
- Each sanitized block is logged as a `RepairRecord` with type `heading_title_text_sanitized` for auditability
- Added 12 tests in `tests/test_heading_title_cleanup_us017.py`: text cleaning applied to heading/title, paragraph blocks unaffected, control chars removed, binary garbage stripped, whitespace collapsed, repair log verification, entity extraction uses cleaned names, claim extraction uses cleaned headings, end-to-end dirty heading flow
- All ACs pass: mypy clean (46 source files), 1201 tests pass
- **Changed files:** `docos/pipeline/normalizer.py`, `tests/test_heading_title_cleanup_us017.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - `_sanitize_heading_title_text()` is called as the FIRST step in `GlobalRepair.repair()` — before heading hierarchy normalization, so `_normalize_heading_hierarchy` also benefits from clean text
  - Blocks other than HEADING and TITLE are intentionally left untouched during normalization — paragraph text cleaning is out of scope for US-017
  - `sanitize_title()` from `docos/slugify.py` is now applied at two stages: normalization (for block text) and compilation (for page titles via `_clean_title()`). This is by design — the normalize-stage cleaning strips noise before extraction, while compile-stage cleaning handles the final output
  - `_normalize_heading_hierarchy` shifts `##` to `#` when headings start at level 2 — tests with single headings must start at level 1 to avoid hierarchy shift side-effects
---

## 2026-04-18 20:30 - US-018
- Added readability gate (`is_readable_title()`) to `RuleBasedEntityExtractor.extract_entities()` in `docos/knowledge/extractor.py`
- Both document entity (from TITLE blocks) and concept entities (from HEADING blocks) are now filtered before being appended to the entity list
- Unreadable entities never reach the knowledge store — they are discarded at the extraction layer, before `_stage_extract()` persists anything
- The compile-stage gate in `runner.py._stage_compile()` still acts as a safety net but will no longer catch entities already filtered at extraction
- Added 10 targeted tests in `tests/test_entity_readability_gate_us018.py`: readable entity created, garbage title blocked, garbage heading blocked, CJK passes, control-char heading blocked, mixed readable/garbage, replacement char blocked, empty title no entity, deterministic filtering, persistence payload blocked
- All ACs pass: mypy clean (46 source files), 1211 tests pass
- **Changed files:** `docos/knowledge/extractor.py`, `tests/test_entity_readability_gate_us018.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - The readability gate is now applied at **two** layers: extraction (`extractor.py`) and compile (`runner.py`) — extraction prevents persistence, compile prevents page generation
  - `is_readable_title()` is imported inside `extract_entities()` method (not at module top level) to match the existing pattern in `runner.py`
  - Block model requires `source_parser` and `source_node_id` fields — use `_make_block()` pattern from US-017 tests
  - `\xff` (`ÿ`) and `\xfe` (`þ`) are valid Latin-1 letters and pass `isalnum()` — use `\x00-\x08` control chars or `\ufffd` replacement chars for garbage test data instead
  - `is_readable_title` threshold is 30% (`min_alpha_ratio=0.3`); when constructing noisy test data, ensure readable chars are < 30% of total length
---

## 2026-04-18 09:10 - US-019
- Verified that US-018's `is_readable_title()` gate in `RuleBasedEntityExtractor.extract_entities()` already covers concept entities (EntityType.CONCEPT from HEADING blocks)
- Concept readability gate is applied at extraction layer — unreadable heading names are discarded before entity list is returned
- Compile-stage gate in `runner.py._stage_compile()` acts as secondary safety net for concept pages
- Added 9 targeted tests in `tests/test_concept_readability_gate_us019.py` covering all 3 ACs: readable concept created, garbage concept blocked, CJK concept passes, control chars blocked, mixed readable/garbage, replacement char blocked, persistence payload blocked, deterministic filtering, concept blocking independent of document entity
- All ACs pass: mypy clean (46 source files), 1220 tests pass
- **Changed files:** `tests/test_concept_readability_gate_us019.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - Concept entities share the same extraction path as document entities in `RuleBasedEntityExtractor` — both use `is_readable_title()` gate, applied in the same `extract_entities()` method
  - US-018 and US-019 both test the same extraction gate but from different perspectives: US-018 focuses on entity extraction broadly, US-019 focuses specifically on concept (heading-based) candidates
  - When testing concept entities specifically, filter by `EntityType.CONCEPT` to isolate concept-level behavior from document-level behavior
---

## 2026-04-18 21:00 - US-020
- Created `docos/vault_validator.py` with `validate_vault()` that scans exported wiki pages and detects: empty filenames, unreadable slugs (non-ASCII-safe chars), empty frontmatter titles, control characters in titles, abnormally long or garbled page names (>200 chars or low readability ratio)
- Integrated validator into `quick_verify_papers.py`: runs on each paper's exported vault, per-paper result includes `vault_validation` dict, batch summary includes aggregate stats (total/passed/failed pages, pass rate), markdown output has "Vault Validation" section
- Added 23 targeted tests covering all 3 ACs: frontmatter parsing, individual issue types (empty filename, unreadable slug, empty title, control chars, long name), aggregate result computation, to_dict serialization, multiple issues per page, mixed quality pages
- All ACs pass: mypy clean (47 source files), 1243 tests pass
- **Changed files:** `docos/vault_validator.py`, `scripts/quick_verify_papers.py`, `tests/test_vault_validator_us020.py`, `scripts/ralph/prd.json`
- **未来迭代的学习：**
  - `validate_vault()` takes a `Path` and scans all `.md` files recursively — works on both populated and empty/nonexistent directories
  - Vault validation runs AFTER export but BEFORE verdict classification, so `vault_validation` dict is available in per-paper result alongside `verdict`, `knowledge_sparse`, etc.
  - `wiki_root` was moved before the `try` block in `run_batch()` to ensure it's available for both success and failure paths
  - US-021 will use `vault_validation` results to prevent `usable_wiki_ready` classification when validation fails
  - `_parse_frontmatter_title()` is a lightweight parser that avoids importing yaml — it just scans for `title:` line in the frontmatter block
---
