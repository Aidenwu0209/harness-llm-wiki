## Codebase Patterns
- This repository is Python-first; use `python -m mypy docos/` for type checking and `python -m pytest tests/ -q` for automated verification.
- Core runtime work is split across `docos/cli`, `docos/pipeline`, `docos/wiki`, `docos/lint`, `docos/harness`, and `docos/review`; keep each story focused on one boundary when possible.
- PipelineRunner (docos/pipeline/runner.py) is the unified entry point for full pipeline execution; new stages should be added as methods there.
- PIPELINE_STAGES in docos/models/run.py defines canonical stage order: ingest, route, parse, normalize, extract, compile, patch, lint, harness, gate.
- Config for tests should use inline YAML strings to avoid external file dependencies.
- Only stdlib_pdf and basic_text parsers are available in the registry; router.yaml references external parsers (pymupdf etc.) that are NOT registered.
- ParserRouter now accepts an optional `parser_registry` parameter in __init__ for validate_config(); existing code that doesn't pass it continues to work.
- PDF dual-column detection works by analyzing x-coordinate clusters from Td positioning commands in content streams.

## 2026-04-14 - US-001
- Created PipelineRunner service class (docos/pipeline/runner.py) that orchestrates all 10 pipeline stages
- Added `docos run <file_path>` CLI command with --origin, --tags, --config options
- Added "patch" to PIPELINE_STAGES in run manifest model
- Pipeline stops on failure and reports the failing stage name
- 6 tests: full success, manifest persistence, all stages completed, IR artifact, failure stops early, CLI registration
- **Files changed:** docos/pipeline/runner.py (new), docos/cli/main.py, docos/models/run.py, tests/test_pipeline_runner.py (new)
- **Future iterations learning:**
  - PIPELINE_STAGES must be kept in sync with runner stages
  - mark_stage() raises ValueError for unknown stage names — always check the list first
  - Config for tests: use inline YAML strings, no external config files needed
---

## 2026-04-15 - US-013
- Added `validate_config(registry)` method to ParserRouter that checks all parser names in routes against the registry
- ParserRouter.__init__ now accepts optional `parser_registry` parameter
- Returns list of unresolved parser names; empty list means all good
- 6 tests: valid config, constructor registry, invalid config, mixed, no-registry, dedup
- **Files changed:** docos/pipeline/router.py, tests/test_router_config_validation_us013.py (new), scripts/ralph/prd.json
- **Future iterations learning:**
  - ParserRouter constructor now has optional parser_registry kwarg; don't break callers that omit it
---

## 2026-04-15 - US-014
- Updated pyproject.toml parser extras with comments documenting the strategy (stdlib_pdf/built-in vs external)
- Added `_missing_parser_message()` helper in orchestrator.py with parser-name-to-extra mapping
- Error messages now include install hints like "pip install docos[parser]" or fallback to pyproject.toml reference
- 5 tests: known parser hint, OCR parser hint, unknown parser hint, orchestrator failure, orchestrator unknown
- **Files changed:** docos/pipeline/orchestrator.py, pyproject.toml, tests/test_parser_extras_us014.py (new), scripts/ralph/prd.json
---

## 2026-04-15 - US-015
- Replaced `_detect_dual_column()` stub with real implementation using x-coordinate cluster analysis
- Extracts Td positioning commands from PDF content streams, groups by page, checks for two distinct column clusters
- Heuristic: two x-clusters with gap > 20 points, each cluster having >= 2 positions
- 5 tests: dual-column PDF, single-column PDF, minimal PDF, non-PDF, deterministic
- **Files changed:** docos/pipeline/signal_extractor.py, tests/test_dual_column_us015.py (new), scripts/ralph/prd.json
---

## 2026-04-15 - US-016
- Added `is_formula_heavy` scoring to `_score_route()`: routes with `table_formula_heavy=True` gain +1 when `is_formula_heavy=True`
- Updated `_build_decision()` to include `is_formula_heavy` in matched_signals
- 4 tests: formula selects formula route, non-formula selects text route, score increases, logged in signals
- **Files changed:** docos/pipeline/router.py, tests/test_formula_heavy_us016.py (new), scripts/ralph/prd.json
---

## 2026-04-15 - US-017
- Documented `max_pages` as soft score with code comments in `_score_route()`
- Updated `_build_decision()` to always log ALL 8 key signals (not just truthy ones)
- Updated `RouteLogEntry.to_dict()` to include a `signals` section with all key signals
- 5 tests: max_pages is soft, within-limit scores higher, all signals in matched, signals present when false, persisted log includes signals
- **Files changed:** docos/pipeline/router.py, tests/test_signal_logging_us017.py (new), scripts/ralph/prd.json
---

## 2026-04-15 - US-023
- WikiLinter.lint() loads real wiki pages (Frontmatter), claims (ClaimRecord), entities (EntityRecord), DocIR, Patch, and page bodies
- Tests verify lint processes real persisted artifacts loaded from disk (WikiStore, KnowledgeStore, PatchStore) not empty placeholders
- Synthetic bad patch with SUPPORTED claim lacking evidence anchors produces P1 blocking finding (UNSUPPORTED_CLAIM_NO_EVIDENCE)
- Duplicate entity candidates detected via case-insensitive canonical_name matching (DUPLICATE_ENTITY_CANDIDATES)
- Broken wikilinks in page bodies detected (BROKEN_WIKILINK)
- High blast radius patch without review_required triggers HIGH_BLAST_NO_REVIEW
- 5 tests: real DocIR+knowledge, persisted artifact round-trip, bad claim detection, duplicate entities, broken wikilinks
- **Files changed:** tests/test_lint_real_us023.py (existing), scripts/ralph/progress.txt
- **Future iterations learning:**
  - ClaimRecord model_validator enforces evidence for SUPPORTED claims at construction; use `model_construct()` to bypass for testing bad data
  - EntityRecord uses `canonical_name` field (not `name`); ClaimRecord uses `statement` field (not `object`)
  - WikiLinter._lint_body() only runs when page_bodies dict is provided; it checks wikilinks, anchors, and schema-body mismatch
  - WikiLinter._lint_operational() checks patch blast_radius.pages > 3 without review_required
---

## 2026-04-15 15:30 - US-024
- Enhanced test_harness_linked_from_manifest to use real RunManifest with RunStore (not just simulated path check)
- HarnessRunner.run() already consumes real DocIR, ClaimRecord, EntityRecord, and Patch artifacts from any source
- ReportStore persists HarnessReport to disk; RunManifest.report_artifact_path links to the persisted file
- Test verifies full round-trip: create manifest, run harness, persist report, link in manifest, reload and verify
- 13 tests total across two US-024 test files; all pass alongside 614 total tests
- **Files changed:** tests/test_harness_real_us024.py, scripts/ralph/prd.json
- **Future iterations learning:**
  - HarnessRunner.run() accepts run_id and source_id as explicit params (not from manifest); it's the pipeline runner's job to pass them
  - HarnessReport is a dataclass (not Pydantic), serialized via _report_to_dict() helper in artifact_stores.py
  - PipelineRunner._stage_harness() already correctly saves report via ReportStore and sets manifest.report_artifact_path
  - EntityRecord uses `canonical_name` field; duplicate detection normalizes via .lower().strip()
---

## 2026-04-15 22:30 - US-033
- Added compile coverage tests for parser and benchmark page types
- Created tests/test_compile_parser_benchmark_us033.py with 28 tests covering:
  - ParserPageContent model instantiation (defaults + all fields)
  - BenchmarkPageContent model instantiation (defaults + all fields)
  - compile_parser_page(): tuple return, frontmatter type/ID/title, body sections (version, capabilities, limitations, fallback parsers), page path, source_ids, minimal content
  - compile_benchmark_page(): tuple return, frontmatter type/ID/title, body sections (dataset, evaluation dimensions, parser results), page path, source_ids, minimal content
  - Regression guard: source, entity, concept pages still compile
  - Render verification for both page types
- All acceptance criteria met: mypy passes, 602 tests pass with no regressions
- **Files changed:** tests/test_compile_parser_benchmark_us033.py (new), scripts/ralph/prd.json
- **Future iterations learning:**
  - WikiCompiler compile methods return (Frontmatter, body_str, Path) tuples consistently
  - Parser and benchmark pages use ReviewStatus.NOT_NEEDED (not PENDING like source/entity pages)
  - Parser page path: wiki_dir/parsers/{slug}.md; Benchmark page path: wiki_dir/benchmarks/{slug}.md
  - Optional sections (capabilities, limitations, fallback_parsers, etc.) only appear in body when lists are non-empty
---

## 2026-04-15 23:30 - US-035
- Generated JSON Schema artifacts from Pydantic models: run.schema.json, page.schema.json, patch.schema.json
- Created schemas/generate_schemas.py script for reproducible schema regeneration
- Created tests/test_docs_alignment_us035.py with 13 tests covering:
  - CLI commands documented in README are all registered (10 top-level + 3 review subcommands)
  - Page types in code (8 types) match documented types exactly
  - Parser names (stdlib_pdf, basic_text_fallback) match documented parsers
  - Schema JSON files exist and are valid JSON Schema
- All acceptance criteria met: mypy passes (41 files), 641 tests pass
- **Files changed:** schemas/generate_schemas.py (new), schemas/run.schema.json (new), schemas/page.schema.json (new), schemas/patch.schema.json (new), tests/test_docs_alignment_us035.py (new), scripts/ralph/prd.json
- **Future iterations learning:**
  - Use Pydantic's model_json_schema() to generate JSON Schema artifacts; it handles enums, optionals, and nested models automatically
  - Click CLI commands can be introspected via `cli --help` output for documentation alignment testing
  - schemas/ directory was pre-existing but empty; schema files go there
---


## 2026-04-15 - US-018 through US-025 batch

### US-018: Generate patches for create, update, and delete page changes
- CompiledPage.compute_patch() now returns CREATE_PAGE, UPDATE_PAGE, or DELETE_PAGE patches
- Added DELETE_PAGE to ChangeType enum; CompiledPage accepts `deleted` flag
- compute_patch() always returns a Patch (never None)
- **Files changed:** docos/models/patch.py, docos/wiki/compiler.py, tests/test_patch_types_us018.py

### US-019: Make patch IDs deterministic from canonical content
- Patch IDs now use SHA-256 hash (first 12 hex chars) of canonical content instead of Python hash()
- Same content always produces same patch_id; format: pat_{page_id}_{sha256_hash}
- **Files changed:** tests/test_patch_deterministic_us019.py

### US-020: Implement patch apply and merge lifecycle steps
- Created PatchService (docos/wiki/patch_service.py) coordinating lifecycle with persisted state
- apply_patch, auto_merge, approve_merge all persist via PatchStore and snapshot wiki state via WikiStore
- **Files changed:** docos/wiki/patch_service.py, tests/test_patch_lifecycle_us020.py

### US-021: Implement patch rollback and reject lifecycle steps
- PatchService.rollback() restores prior wiki state; reject() keeps patch auditable
- **Files changed:** tests/test_patch_rollback_us021.py

### US-022: Link review items to concrete patch IDs
- ReviewItem.target_object_id stores patch_id; ReviewAction.linked_patch_id links actions
- **Files changed:** tests/test_review_patch_link_us022.py

### US-023: Make lint load real wiki, knowledge, and patch artifacts
- Fixed PipelineRunner._stage_lint() to load real wiki pages from WikiStore (was passing pages=[])
- **Files changed:** docos/pipeline/runner.py, tests/test_lint_real_artifacts_us023.py

### US-024: Make harness consume real parse and knowledge artifacts
- Tests verify harness reads real DocIR, claims, entities, patches and produces metrics
- **Files changed:** tests/test_harness_real_artifacts_us024.py

### US-025: Enforce release gate rules and report gate decisions
- ReleaseGate.check() already fully implemented; tests already present
- **Files changed:** scripts/ralph/prd.json

- **Future iterations learning:**
  - DocIR requires `doc_id` field (not just source_id)
  - EvidenceAnchor requires anchor_id, source_id, doc_id as required fields
  - ClaimRecord with SUPPORTED status MUST have evidence_anchors (model_validator enforces)
  - HarnessRunner citation coverage counts ALL claims; ensure all have evidence for 95%+ threshold
  - Patch.stage() recalculates review_required; manual setting gets overridden
  - WikiStore uses sanitized page_path as key (slashes to underscores)
---

## 2026-04-15 - US-030
- Enhanced tests/test_rerun_stability_us030.py from 5 to 9 tests covering all acceptance criteria
- Added test_stable_patch_ids_on_rerun: verifies patch_id values are identical across two pipeline runs with same fixture
- Added test_stable_patch_ids_via_persisted_artifacts: verifies persisted patch artifacts (via PatchStore) have stable IDs
- Added test_failure_path_debug_artifacts_preserved: verifies all artifact paths referenced in manifest exist on disk and manifest is reloadable
- Added test_rerun_produces_comparable_artifacts: verifies both runs produce complete artifact sets (manifests, knowledge) with equal entity/claim counts
- All 9 tests pass; mypy clean (41 files); 659 total tests pass
- **Files changed:** tests/test_rerun_stability_us030.py, scripts/ralph/prd.json
- **Future iterations learning:**
  - PatchStore saves as `<patch_id>.json` but manifest.patch_artifact_path does NOT include `.json` suffix
  - RunStore persists manifests at `<base_dir>/manifests/<run_id>.json` (not `runs/`)
  - CompiledPage.compute_patch() uses SHA-256 of body content → deterministic patch_id for same content regardless of run_id
  - KnowledgeExtractionPipeline produces deterministic entity_id/claim_id values from same DocIR content
---
## 2026-04-15 23:45 - US-036
- Created tests/test_golden_loop_us036.py with 8 tests validating the full golden closed loop
- Test 1 (test_golden_run_persists_all_artifacts): Runs simple_text fixture through full pipeline and verifies ALL artifacts persisted — route, DocIR, knowledge, patch, lint, harness, wiki state, route logs
- Test 2 (test_report_cli_shows_golden_run_info): `docos report <run_id>` displays route, parser chain, gate decision, and all artifact locations
- Tests 3-5: Re-running same fixture preserves stable entity_id, claim_id, and relation_id values
- Test 6 (test_rerun_preserves_artifact_structure): Rerun produces same page count, block count, entity/claim/relation counts, and same route decision
- Test 7 (test_rerun_does_not_regress_golden_path): Gate decision, harness pass/fail, and lint findings count are stable across reruns
- Test 8 (test_golden_report_shows_all_artifact_locations): Report output includes paths for all persisted artifacts
- All acceptance criteria met: mypy passes (41 files), 676 tests pass with no regressions
- **Files changed:** tests/test_golden_loop_us036.py (new), scripts/ralph/prd.json
- **Future iterations learning:**
  - The existing US-029 (test_golden_pipeline_us029.py) and US-030 (test_rerun_stability_us030.py) cover overlapping territory; US-036 consolidates and extends with report CLI verification
  - Report CLI output uses both `parser_chain` (dict) and `parser_chain_list` (list) keys; test should handle both formats
  - The `monkeypatch.chdir(tmp_path)` pattern is needed for report CLI tests because the CLI uses `Path(".")` as base
  - All stores use absolute paths from manifest, so report CLI works correctly after chdir to tmp_path
---

## 2026-04-15 02:30 - US-026 through US-036 batch

### US-026: Add raw fixtures for simple, complex, and OCR-like inputs
- Created tests/fixtures/ directory with build_fixtures.py module
- Three PDF builders: _build_simple_pdf (single-column text), _build_dual_column_pdf (two-column layout), _build_ocr_like_pdf (image-heavy with 5 image streams)
- get_fixture() and get_all_fixtures() API for test access
- 10 tests: fixture creation, content verification, signal extraction compatibility

### US-027: Add integration tests for raw fixture route and parse stages
- Route integration: each fixture routed through ParserRouter with config
- Parse integration: each fixture parsed through PipelineOrchestrator
- Full pipeline integration via PipelineRunner on all three fixtures
- 9 tests covering route decisions, parse results, and multi-fixture routing

### US-028: Add integration tests for normalize and extract from raw fixtures
- Normalize tests: fixture -> parse -> normalize -> persisted DocIR
- Extract tests: fixture -> parse -> normalize -> extract -> persisted knowledge
- 9 tests covering DocIR persistence, knowledge artifact persistence, valid structure

### US-029: Add a full pipeline to report integration test
- Golden-path test: simple_text through full pipeline to report
- Verifies all artifacts persisted and report includes route, parser, gate decision
- 2 tests

### US-030: Add rerun stability tests for patch and knowledge IDs
- Stable entity IDs, claim IDs, relation IDs, and entity names across reruns
- Debug artifacts availability verification
- 5 tests (pre-existing file was enhanced by another agent to 9 tests)

### US-031: Add runtime entrypoints for domain skills
- Created docos/skills_mapping.py with SKILL_ENTRYPOINTS mapping
- Created .agents/skills/skills_entrypoints.json as source of truth
- 8 tests: module existence, Click command registration, JSON file validity, mapping consistency

### US-032: Add contract tests for domain skills
- SKILL_CONTRACTS dict defining input, output, invariants, fallback, evaluation for all 6 skills
- Invariant verification tests: deterministic routing, schema validation, deterministic IDs, atomic patches, read-only lint, append-only review
- 14 tests covering all contract aspects

### US-033: Add compile coverage for parser and benchmark page types
- Tests for compile_parser_page and compile_benchmark_page
- Regression tests for existing page types (source, entity, concept, failure, comparison, decision)
- 14 tests

### US-034: Expand run observability and debug summaries
- Tests verifying RunManifest records: selected_route, parser_chain, fallback_used, lint_summary, harness_summary, gate_decision, review_status
- Stage duration_seconds verification
- 9 tests (code changes for populating observability fields were done by another agent)

### US-035: Align README and schema artifacts with implemented behavior
- Updated README to mention all 8 page types including "comparison"
- Tests for: README mentions pipeline stages, page types, parsers, CLI commands exist
- Tests for: run/page/patch schema JSON files exist, valid, match Pydantic models
- 17 tests

### US-036: Validate the golden closed loop from raw fixture to stable rerun
- Golden fixture -> full pipeline -> verify all 8 artifact types persisted
- Report trace verification: route, parser chain, gate decision, artifact locations
- Rerun stability: entity IDs, claim IDs, relation IDs, patch IDs all stable
- Non-regression: both runs complete with same structure
- 5 tests

- **Files changed (this batch):**
  - tests/fixtures/__init__.py (new)
  - tests/fixtures/build_fixtures.py (new)
  - tests/test_raw_fixtures_us026.py (new)
  - tests/test_route_parse_integration_us027.py (new)
  - tests/test_normalize_extract_integration_us028.py (new)
  - tests/test_golden_pipeline_us029.py (new)
  - tests/test_rerun_stability_us030.py (new, by other agent)
  - docos/skills_mapping.py (new)
  - .agents/skills/skills_entrypoints.json (new)
  - tests/test_skill_entrypoints_us031.py (new)
  - tests/test_skill_contracts_us032.py (new)
  - tests/test_compile_page_types_us033.py (new)
  - tests/test_observability_us034.py (new)
  - tests/test_readme_alignment_us035.py (new)
  - tests/test_golden_closed_loop_us036.py (new)
  - README.md (updated)
  - scripts/ralph/prd.json (updated)
- **Final state:** 698 tests pass, mypy clean (41 files)
- **Future iterations learning:**
  - Test config YAML strings can be extracted into a shared helper to reduce duplication across test files
  - PipelineRunner needs configs/ directory with router.yaml; always create in tmp_path test setups
  - SKILL_ENTRYPOINTS in docos/skills_mapping.py must stay in sync with .agents/skills/skills_entrypoints.json
  - Dual-column fixture uses x-offsets (72 and 320) in Td commands; signal extractor needs both clusters for detection
  - OCR-like fixture uses /Subtype /Image with large /Length streams to trigger is_scanned=True detection
---

## 2026-04-15 - US-034
- Added observability fields to RunManifest: selected_route, parser_chain, fallback_used, lint_summary, harness_summary, gate_decision, review_status
- Added duration_seconds and warnings fields to PipelineStage model
- mark_stage() now auto-calculates duration_seconds when stage completes or fails
- Added add_stage_warning() helper method to RunManifest
- Updated PipelineRunner to populate all observability fields during pipeline execution (route, parse, lint, harness, gate stages)
- Updated report CLI command to read observability fields from manifest (with fallback to artifact files for backward compat)
- Report output now includes duration_seconds and warnings per stage
- Kept backward compat fields (route dict, parser_chain dict, harness_status) for existing US-004/US-012 tests
- 29 tests covering: field population, duration recording, warnings, serialization round-trip, RunStore persistence, report CLI output
- **Files changed:** docos/models/run.py, docos/pipeline/runner.py, docos/cli/main.py, tests/test_observability_us034.py, scripts/ralph/prd.json
- **Future iterations learning:**
  - Report CLI has backward compat expectations: `route` (dict), `parser_chain` (dict with primary/fallbacks), `harness_status` — always keep these when adding new fields
  - PipelineStage model changes affect all tests that create manifests — duration_seconds and warnings have safe defaults (None and [])
  - When adding manifest fields, update both the runner (to populate) AND the report CLI (to display) to keep them in sync
---
